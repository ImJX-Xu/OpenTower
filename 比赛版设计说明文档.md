# OpenTower Linux Ops 项目说明

## 1. 项目概述

OpenTower Linux Ops 是一个面向 Linux 运维场景的自然语言智能代理系统。系统采用 CLI-first 交互方式，允许用户直接用自然语言描述需求，由系统完成请求路由、风险判断、命令规划、命令执行和结果反馈。

项目的重点不是“生成一段看起来很聪明的 shell”，而是把自然语言真正接入 Linux 运维任务，同时保持执行链路的可控性、可解释性和可验证性。当前版本已经具备：

- 真实命令执行，而不是仅返回建议命令
- 明确的多 agent 分工，而不是单一黑箱
- 危险操作阻断与确认流
- 真实日志、转录、输出和确认记录
- 自动化测试、语料回放评测、WSL 端到端 smoke 与大样本真实执行验证

当前系统围绕固定四 agent 工作流构建：

- `intent-parser`
- `security-guard`
- `command-planner`
- `result-analyst`

在此基础上，本版本进一步加入了受边界约束的 LLM 辅助解析能力，用于提升自然语言兼容性，但不让模型直接决定任意命令执行。

## 2. 设计原则

### 2.1 自然语言优先，但执行面收敛

系统默认把自然语言作为正式入口。用户可以直接输入“查看磁盘使用情况”“检查 sshd 服务状态”“show cpu usage”这类请求，不需要先学习复杂命令参数。

与此同时，系统不会把自然语言直接映射为任意 shell，而是把执行面收敛到一组已实现、已测试、可审计的 Linux 运维操作。

### 2.2 安全前置，而不是事后补救

运维场景与普通聊天不同。错误的命令可能直接影响系统状态，因此安全检查必须发生在执行前，而不是执行后再解释。系统对每个请求都先做结构化风险判断，再决定允许、确认或阻断。

### 2.3 多 agent 分工必须有工程意义

本方案中的多 agent 并不是展示概念，而是把完整链路拆成职责清晰的几个阶段：

- 理解用户意图
- 判断风险与边界
- 生成受控命令计划
- 把命令结果转换为可读反馈

这种拆分便于测试、审计、定位问题，也更适合在真实系统场景中稳定运行。

### 2.4 LLM 只能做边界内增强，不能越权扩展

本版本新增了两类模型辅助能力：

- `llm_normalizer`：把同义改写、口语化表达映射回已实现操作
- `fallback_research`：只在低风险只读场景下做补救式解析

这两层都受严格边界约束。模型不能凭空发明新能力，也不能把不支持的高风险请求包装成可执行命令。

## 3. 总体架构

### 3.1 执行主线

系统主链路如下：

```text
用户自然语言输入
  -> CLI / Console 入口
  -> 路由解析链
       -> local_rule
       -> llm_normalizer
       -> fallback_research
  -> intent-parser
  -> security-guard
  -> command-planner
  -> linux executor
  -> result-analyst
  -> 自然语言反馈 + 日志/转录/输出文件
```

其中：

- CLI 是正式执行入口
- Console 是持续交互入口
- `/` 开头输入表示显式命令模式
- 非 `/` 输入默认按自然语言任务处理

### 3.2 三层路由解析链

本版本最重要的增强之一，是把自然语言解析从单一路由扩展为三层链路：

#### `local_rule`

第一层是本地确定性规则解析。它负责覆盖当前正式支持的核心语料和稳定表达，是默认优先路径。

特点：

- 完全可预测
- 运行稳定
- 便于单测与回放评测

#### `llm_normalizer`

当本地规则无法命中时，系统会尝试使用结构化意图归一化组件，将请求重新映射到已经存在的工作流和操作。

特点：

- 只允许映射到已实现操作
- 输出必须是结构化 JSON
- 不允许扩展未实现功能
- 适合处理同义表达、英文表达和自然口语改写

例如，某些未被本地规则直接覆盖的服务状态表达，可以被归一化到 `service_status`。

#### `fallback_research`

如果前两层仍无法安全解析，系统会进入只读 fallback 研究路径。该路径只覆盖少量低风险运维观察类任务，不负责写操作。

当前允许的 fallback 操作包括：

- `top_cpu`
- `top_memory`
- `load_average`
- `uptime_summary`
- `service_status`
- `tail_log`
- `recent_error_scan`

该组件的目标是提高系统对真实自然语言输入的容错性，同时保持安全边界不被破坏。

### 3.3 结构化路由结果

为了避免“不支持时直接抛出红色错误”的体验，本版本的 dispatch 结果统一输出以下字段：

- `resolution_status`
- `resolution_source`
- `resolution_reason`

其中：

- `resolution_status` 表示请求是否进入正式支持链路
- `resolution_source` 标记命中来源，如 `local_rule`、`llm_normalizer`、`fallback_research`
- `resolution_reason` 给出支持或拒绝的原因

这使系统在不支持场景下也能返回清晰、可解释的结构化结果。

## 4. 分层实现

### 4.1 交互层

负责接收输入与展示输出，包括：

- `python -m opentower_cli ...`
- `python -m opentower_cli console`
- `workflow`
- `dispatch`
- `provider-status`
- `auth`

当前正式对外暴露的命令面只有：

- `workflow`
- `dispatch`
- `console`
- `provider-status`
- `auth`

设计目标是保持入口简洁，减少无关命令面，突出自然语言作为正式交互方式。

### 4.2 编排层

负责把一次请求组织成完整工作流，包括：

- 路由解析
- agent 顺序编排
- 确认流状态管理
- 运行日志与产物路径组织

这一层确保每一轮请求都形成闭环，而不是只返回一个孤立结果。

### 4.3 执行层

负责 Linux 命令执行，包括：

- 本地 bash / WSL 执行
- 超时控制
- 返回码处理
- 标准输出与错误输出收集

执行层与自然语言理解层分离，避免模型直接接管 shell 环境。

### 4.4 持久化与审计层

系统会把关键运行信息写入 `production/`，包括：

- `session-logs/`
- `session-outputs/`
- `session-transcripts/`
- `group-chat-recaps/`
- `pending-confirmations/`
- `eval-reports/`

这使系统具备可审计、可回放、可复核的工程基础。

## 5. 核心组件与职责

### 5.1 Intent Parser

对应文件：`runtime/opentower_cli/intent_parser.py`

职责：

- 将自然语言路由到支持的工作流
- 抽取结构化实体
- 区分读操作与写操作
- 提前识别路径、端口、用户、服务等关键对象

当前可抽取的典型实体包括：

- 路径
- 端口
- 用户名
- 用户组
- 文件名模式
- 搜索类型
- 服务名

### 5.2 Security Guard

对应文件：`runtime/opentower_cli/security_agent.py`

职责：

- 对解析后的意图做风险判断
- 对极高风险操作直接阻断
- 对高风险操作进入确认流
- 输出风险原因与潜在影响

当前风险决策分为三类：

- `allow`
- `confirm`
- `block`

### 5.3 Command Planner

对应文件：`runtime/opentower_cli/command_generator.py`

职责：

- 把结构化意图转换为受控 Linux 命令计划
- 区分预览命令与执行命令
- 限制命令集合在正式支持范围内

相比“让模型自由生成 shell”，当前方案的优点是：

- 命令可预测
- 行为可测试
- 演示更稳定
- 安全边界更清晰

### 5.4 Result Analyst

对应文件：`runtime/opentower_cli/feedback_agent.py`

职责：

- 解析命令输出
- 把原始结果转成自然语言反馈
- 为不同工作流生成定制化总结
- 在确认流里生成用户可理解的下一步说明

输出的目标不是简单回显终端原文，而是提供带结构、可复核的反馈。

### 5.5 Intent Normalizer

对应文件：`runtime/opentower_cli/intent_normalizer.py`

这是本版本新增的关键组件之一。它的作用是在本地规则未命中时，将请求映射到已经存在的工作流和操作，而不是生成自由命令。

约束机制包括：

- 只允许落到预定义工作流和操作
- 缺少关键实体时拒绝映射
- 映射结果必须结构化
- 对不支持请求优先返回 `unsupported`

这让系统能够处理更自然的表达方式，同时不牺牲可控性。

### 5.6 Fallback Research Agent

对应文件：`runtime/opentower_cli/fallback_research_agent.py`

这是本版本新增的第二个关键组件。它只覆盖低风险只读观察类任务，用于提高在真实 Linux 环境中的自然语言兼容性。

当前可处理的典型请求包括：

- `show cpu usage`
- `show load average`
- `show uptime`
- `tail the latest syslog log`
- `tail the latest nginx error log`
- `check sshd service status`

它不会处理以下类型请求：

- 重启、停止、启动服务
- 安装软件
- 包管理
- 防火墙修改
- 用户写操作
- 任何越出正式边界的系统变更

## 6. 功能范围

### 6.1 磁盘与存储检查

支持能力：

- `disk_usage`
- `disk_usage_with_logs`

典型请求：

- `查看磁盘使用情况`
- `哪个分区快满了`
- `检查服务器存储空间`

主要命令：

- `df -h`
- `lsblk`
- `du -sh /var/log/*`

输出特点：

- 自动总结挂载点
- 自动识别高占用分区
- 可结合 `/var/log` 占用信号提示存储压力

### 6.2 文件检索与日志读取

支持能力：

- `filename_search`
- `content_search`
- `inspect_permissions`
- `tail_log`
- `recent_error_scan`

典型请求：

- `找到所有 nginx 配置文件`
- `搜索包含 "database" 的文件`
- `检查 /etc/nginx 的权限`
- `tail the latest syslog log`
- `scan recent auth errors`

主要命令：

- `find ... -type f -name ...`
- `find ... -type d -name ...`
- `grep -R -n -i ...`
- `tail -n ...`
- `grep -i -E 'error|fail|crit|panic|fatal|denied' ... | tail -n ...`

输出特点：

- 区分文件名检索与内容检索
- 区分普通文件搜索与日志读取
- 支持安全的只读日志尾部查看与错误扫描

### 6.3 进程、服务与系统状态检查

支持能力：

- `port_lookup`
- `top_memory`
- `service_status`
- `top_cpu`
- `load_average`
- `uptime_summary`

典型请求：

- `哪些进程占用 80 端口`
- `查看 nginx 服务状态`
- `显示占用内存最多的 5 个进程`
- `show cpu usage`
- `show load average`

主要命令：

- `lsof -i :<port>`
- `ss -ltnp`
- `systemctl status <service> --no-pager`
- `ps -C <service> -o pid=,comm=,args=`
- `ps aux --sort=-%mem | head -6`
- `ps aux --sort=-%cpu | head -6`
- `uptime`

输出特点：

- 支持端口、服务、资源使用三类观察
- 服务状态查询优先使用 `systemctl`，必要时回退到进程视角
- 这一实现避免了简单 `grep` 方式容易把自身查询进程误识别为目标服务的问题

### 6.4 用户管理

支持能力：

- `list_users`
- `inspect_user`
- `create_user`
- `add_user_to_group`
- `delete_user`
- `batch_delete_users`

典型请求：

- `列出本机用户`
- `创建用户 dev01`
- `给用户 dev01 加入 docker 组`
- `删除用户 test_user`

主要命令：

- `getent passwd`
- `id`
- `groups`
- `useradd -m -s /bin/bash`
- `usermod -aG`
- `userdel -r`

输出特点：

- 覆盖常见用户管理动作
- 对批量删除采用先预览、后确认模式

## 7. 安全机制设计

### 7.1 直接阻断

对于极高风险请求，系统直接拒绝执行，例如：

- 删除 `/etc`、`/boot`、`/sys`、`/proc` 等关键目录
- 删除受保护账户

目标不是提醒一下，而是立即停止执行。

### 7.2 二次确认

对于高风险但不属于必须直接阻断的操作，系统进入确认流，例如：

- 删除普通路径
- 递归权限变更
- 批量删除用户

确认输出包含：

- `confirmation_id`
- 风险等级
- 风险原因
- 潜在影响
- 继续执行路径
- 取消执行路径

### 7.3 只读 fallback 安全边界

本版本新增的 fallback 路径明确限定为只读场景。即使模型能理解请求，也不能把以下操作包装成 fallback 成功：

- `restart nginx service`
- `stop docker service`
- `install nginx`
- `upgrade openssl`
- `reboot the machine`

这些请求仍然会安全拒绝。

### 7.4 不支持场景的结构化处理

此前自然语言越界时，系统更容易表现为原始解析失败。本版本改为统一返回结构化 unsupported 结果，明确告诉调用方：

- 当前请求是否支持
- 是在哪一层被拒绝
- 为什么被拒绝

这显著改善了错误可解释性，也更适合真实交互和现场展示。

### 7.5 审计闭环

确认完成后，系统会把最终结果回写到主运行日志，避免日志停留在中间态。这样每一次高风险请求都有完整的可追踪证据链。

这一轮收敛里，确认记录还进一步持久化了完整的 `intent entities`、风险评估信息和 `parser_kind`。这意味着确认后的执行不再依赖弱重建，而是基于原始结构化上下文回放，减少了确认前后能力漂移的风险。

同时，系统把 operation 元数据收口到了统一 catalog，normalizer、fallback 和 confirmation replay 共享同一份能力定义，降低了多处手工维护导致的边界不一致问题。

## 8. LLM 接入方式

### 8.1 `auth.json`

系统默认从仓库根目录读取 `auth.json` 作为 LLM / provider 配置文件。仓库同时提供 `auth.example.json` 作为模板。

`auth.json` 的定位是：

- 本地真实配置文件
- 不进入版本库
- 供 console、provider 检查、intent normalizer 和 fallback research 使用

### 8.2 支持的 provider

当前支持：

- `anthropic`
- `openai-compatible`
- `ollama`

其中：

- `anthropic` 适合云端模型
- `openai-compatible` 适合 DeepSeek / OpenAI / OpenRouter / SiliconFlow 等兼容接口
- `ollama` 适合本地模型

### 8.3 OpenAI-compatible 自动模型发现

本版本对 `openai-compatible` 接入做了重要增强：

- 当用户未显式指定模型时，系统会尝试读取 provider 的 `/models`
- 如果 provider 返回模型列表，系统会自动挑选合适的 chat 模型
- 如果模型列表不可用，系统再回退到默认模型

这项增强解决了 DeepSeek 等兼容接口下“只配置了 base URL 和 key，但没写 model 时运行容易报错”的问题。

### 8.4 LLM 的角色边界

在本系统中，LLM 的职责是：

- 做结构化意图归一化
- 做边界内只读 fallback 补救

LLM 不负责：

- 直接生成任意 shell
- 绕过安全规则
- 扩展未实现工作流
- 决定越界写操作

因此模型增强的是语言兼容性，而不是执行权限。

## 9. 使用方式

### 9.1 顶层 CLI：自然语言优先

当前顶层 CLI 默认优先处理自然语言，不需要先显式写 `dispatch`。

例如：

```bash
python -m opentower_cli 查看磁盘使用情况
python -m opentower_cli 找到所有 nginx 配置文件
python -m opentower_cli 检查 sshd 服务状态
python -m opentower_cli show cpu usage
python -m opentower_cli show load average
```

### 9.2 显式命令模式

如果需要明确控制模式，可以使用：

```bash
python -m opentower_cli /workflow
python -m opentower_cli /provider-status
python -m opentower_cli /auth
```

### 9.3 显式 dispatch

也可以直接使用：

```bash
python -m opentower_cli dispatch --objective "show cpu usage" --execute
python -m opentower_cli dispatch --objective "check sshd service status" --execute
python -m opentower_cli dispatch --objective "restart nginx service" --execute
```

最后一个例子会返回 `resolution_status: unsupported`，而不是进入危险执行。

### 9.4 交互式 Console

启动方式：

```bash
python -m opentower_cli console
```

Console 中默认输入都按自然语言处理，只有以 `/` 开头时才进入显式命令模式。

## 10. 验证结果

截至 `2026-04-26`，项目已经完成以下验证：

### 10.1 自动化测试

```bash
python -m pytest -q
```

结果：

- `95 passed`

### 10.2 自然语言语料回放评测

```bash
python scripts/run_nl_eval.py --fixture-profile extended
```

结果：

- `2009/2009 passed`

这套评测语料覆盖了：

- 中文与英文自然语言请求
- 支持场景与不支持场景
- allow / confirm / block 风险分布
- 本地规则解析与安全决策边界

同时，语料扩充以版本化 JSONL 分层评测集形式落地：

- `extended`：完整 2009 条回放
- `core`：416 条高频 PR/CI 基准集
- `model`：228 条带模型链路验证集

可由 `scripts/generate_nl_eval_seed.py` 生成，并通过 `scripts/run_nl_eval.py` 在纯本地规则模式或 `--with-model` 模式下回放验证；如果某类表达在评测集中持续稳定命中，才进一步通过 parser、normalizer 或 fallback agent 的代码评审进入正式运行时能力。

### 10.3 WSL 真实执行 smoke

在真实 WSL Linux 环境中，本版本已完成只读端到端 smoke 验证，成功场景包括：

- `show cpu usage`
- `show load average`
- `tail the latest syslog log`
- `check sshd service status`

同时验证了高风险写请求会被安全拒绝：

- `restart nginx service`
- `create user smokeuser` 会进入确认流
- `将用户 dev01 加入 sudo 组` 会进入确认流

这轮 smoke 现在已经收敛为 `scripts/run_wsl_smoke.py` 一键脚本，当前结果为 `10/10 passed`。此外，项目还在真实 WSL 环境中完成了一轮 `543` 条真实执行验证，并据此整理出整数版 `500` 条报告，结果为 `499/500 passed`。

这说明新增的 fallback 路径、统一确认流和架构收敛后的确认回放能力已经能够在真实 Linux 执行面上工作，而不是只停留在离线解析阶段。

## 11. 项目特点与优势

### 11.1 不是聊天演示，而是真实运维执行链

很多方案停留在“自然语言转命令建议”或“模型回答 shell”。OpenTower Linux Ops 提供的是完整执行链：

- 请求进入正式 CLI
- 意图进入结构化路由
- 风险在执行前被判定
- 命令经过受控规划
- 结果经过结构化反馈
- 全流程保留日志与转录

### 11.2 安全边界清晰

系统不会因为自然语言表达更复杂，就放弃安全约束。高风险写操作仍然被确认或阻断；新增 LLM 组件也必须在既定边界内工作。

### 11.3 语言兼容性更强

本版本的核心升级之一，是在不扩大执行权限的前提下，提高了对真实自然语言输入的兼容性：

- 本地规则负责稳定命中
- `llm_normalizer` 负责同义改写归一化
- `fallback_research` 负责少量只读补救
- `--with-model` 回放脚本可直接验证真实 provider 链路，而不只验证本地 parser

这使系统不再严格受限于最初的小语料表达方式。

### 11.4 演示稳定性更高

纯模型自由生成命令，在现场环境里风险和波动都很高。当前方案通过固定工作流、固定命令规划和结构化风控，显著提高了演示稳定性。

### 11.5 多 agent 拆分有真实工程价值

四个 agent 分别承担理解、风控、规划和反馈职责，新增的 normalizer 与 fallback 又以受控方式增强语言兼容性。这样既保留了多 agent 架构的完整性，也保持了工程可测性。

### 11.6 具备继续扩展的基础

当前范围虽聚焦比赛核心要求，但系统已经具备：

- provider 接入层
- 路由增强层
- 安全判定层
- 可扩展工作流目录
- 日志与评测基础设施

后续可以继续扩展更多低风险观察类工作流，或在严格边界下增加新的正式能力。

## 12. 当前边界

为了保证稳定性与安全性，当前系统明确不追求大而全。当前边界包括：

- 聚焦固定 Linux 运维工作流
- 不支持任意 shell 自由生成
- 不支持部署、安装、包管理、重启、停止、启动、防火墙修改等越界写操作
- fallback 路径只处理低风险只读场景
- 远程执行接口保留扩展空间，但当前正式执行以本地 Linux / WSL 为主

这种边界控制不是能力缺失，而是为了保证系统在真实运维场景中的可控性与可信度。

## 13. 推荐现场展示流程

为了完整体现系统能力，可以按以下顺序展示：

1. `查看磁盘使用情况`
2. `找到所有 nginx 配置文件`
3. `哪些进程占用 80 端口`
4. `show cpu usage`
5. `show load average`
6. `tail the latest syslog log`
7. `check sshd service status`
8. `给所有文件 777 权限`，展示确认流
9. `restart nginx service`，展示结构化拒绝
10. `/provider-status` 或 `/auth`，展示接入状态可见

这组流程能够覆盖：

- 基础运维能力
- 自然语言兼容性增强
- 安全机制
- 只读 fallback 能力
- 真实执行与可解释反馈

## 14. 结语

OpenTower Linux Ops 的重点不在于让模型显得更自由，而在于把自然语言真正落到 Linux 运维执行场景中，同时保留系统级操作所需要的可控性、可解释性和可验证性。

本版本在原有基础上进一步补强了自然语言兼容性和真实环境可用性：

- 新增三层路由解析链
- 新增结构化 unsupported 返回
- 新增只读 fallback 观察能力
- 新增 openai-compatible 自动模型发现
- 新增 2009 条语料生成与评测回放
- 新增 `core / extended / model` 分层评测集
- 新增 `--with-model` 真实模型链路回放
- 新增统一的用户变更确认流
- 新增统一的 operation catalog 与确认上下文持久化
- 完成真实 WSL 端到端 smoke 验证
- 完成 WSL 真实执行大样本验证，整数版 `500` 条报告达到 `499/500 passed`

因此，这不是一个“会说运维术语”的聊天工具，而是一套已经具备真实执行、风险控制、结构化反馈和审计闭环能力的 Linux 运维自然语言代理系统。
